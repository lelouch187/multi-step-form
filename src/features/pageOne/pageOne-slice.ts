import { createSlice } from '@reduxjs/toolkit'



interface SideState {
  
}

const initialState: SideState = {
   
}

export const pageOneSlice = createSlice({
  name: 'pageOne',
  initialState,
  reducers: {

  },
})

export const {  } = pageOneSlice.actions



export default pageOneSlice.reducer